<UserControl x:Class="FreeFlight.CabinControl.App.Views.AirlinersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="820"
             d:DesignWidth="1200">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="152" />
                <RowDefinition Height="16" />
                <RowDefinition Height="44" />
                <RowDefinition Height="16" />
                <RowDefinition Height="400" />
                <RowDefinition Height="28" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}" />
                    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Ellipse Width="13" Height="13" Fill="{StaticResource WarningBrush}" Margin="0,0,10,0" />
                    <TextBlock Text="{Binding Status.ConnectionLabel}" Foreground="{StaticResource WarningBrush}" FontSize="14" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="20,16">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="34" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <TextBlock Text="Active Airline" Style="{StaticResource SectionTitleStyle}" />
                        <Button Content="Change Airline"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding ChangeAirlineCommand}"
                                HorizontalAlignment="Right"
                                MinHeight="34"
                                Padding="16,5" />
                    </Grid>
                    <Grid Grid.Row="1" Margin="4,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="1.35*" />
                            <ColumnDefinition Width="0.72*" />
                            <ColumnDefinition Width="1.25*" />
                            <ColumnDefinition Width="0.62*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="82" Height="72" CornerRadius="9" HorizontalAlignment="Left" Background="#102E5C" BorderBrush="#2487FF" BorderThickness="1">
                            <TextBlock Text="{Binding ActiveAirline.Icao}" FontSize="22" FontStyle="Italic" FontWeight="Bold" Foreground="#5BA9FF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ActiveAirline.Name}" FontSize="22" FontWeight="SemiBold" />
                            <TextBlock Text="SOURCE" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="0,9,0,2" />
                            <TextBlock Text="Local catalog" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="ICAO" Foreground="{StaticResource TextMutedBrush}" FontSize="10" />
                            <TextBlock Text="{Binding ActiveAirline.Icao}" FontSize="17" Margin="0,5,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="3" VerticalAlignment="Center">
                            <TextBlock Text="SOUND PACK" Foreground="{StaticResource TextMutedBrush}" FontSize="10" />
                            <TextBlock Text="{Binding ActiveAirline.SoundPack}" FontSize="15" Margin="0,5,12,0" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <StackPanel Grid.Column="4" VerticalAlignment="Center">
                            <TextBlock Text="STATUS" Foreground="{StaticResource TextMutedBrush}" FontSize="10" />
                            <TextBlock Text="ACTIVE" Foreground="{StaticResource SuccessBrush}" FontSize="16" Margin="0,5,0,0" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions><ColumnDefinition Width="330" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <Grid>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Padding="34,7,12,7"
                             ToolTip="Search airline profiles by airline name or ICAO code" />
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="Search airlines by name or ICAO..." Foreground="{StaticResource TextMutedBrush}" Margin="8,0,0,0" />
                    </StackPanel>
                </Grid>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0">
                    <Button Content="All" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding FilterCommand}" CommandParameter="All" MinHeight="40" Padding="20,5" />
                    <Button Content="Real-world" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding FilterCommand}" CommandParameter="Real-world" MinHeight="40" Padding="18,5" Margin="8,0,0,0" />
                    <Button Content="Virtual Airlines" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding FilterCommand}" CommandParameter="Virtual Airlines" MinHeight="40" Padding="18,5" Margin="8,0,0,0" />
                    <Button Content="Installed Packs" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding FilterCommand}" CommandParameter="Installed Packs" MinHeight="40" Padding="18,5" Margin="8,0,0,0" />
                    <TextBlock Text="{Binding ActiveFilter}" Style="{StaticResource MutedTextStyle}" Margin="18,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="280" /></Grid.ColumnDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,16,0">
                    <ItemsControl ItemsSource="{Binding VisibleAirlines}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="272" Height="176" Margin="0,0,12,12" Padding="15" CornerRadius="9" BorderThickness="1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#99071426" />
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Background" Value="#B20B2244" />
                                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.RowDefinitions><RowDefinition Height="72" /><RowDefinition Height="1" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="64" /><ColumnDefinition Width="*" /><ColumnDefinition Width="64" /></Grid.ColumnDefinitions>
                                            <Border Width="50" Height="58" CornerRadius="7" Background="#102E5C" BorderBrush="#2B87EE" BorderThickness="1" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Icao}" FontSize="13" FontWeight="SemiBold" Foreground="#5CAEFF" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding Icao}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0" />
                                            </StackPanel>
                                            <Button Grid.Column="2"
                                                    Content="Select"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Command="{Binding DataContext.SelectAirlineCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    MinHeight="32"
                                                    Padding="9,3"
                                                    VerticalAlignment="Center" />
                                        </Grid>
                                        <Rectangle Grid.Row="1" Fill="{StaticResource BorderSoftBrush}" />
                                        <Grid Grid.Row="2" Margin="0,10,0,0">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="0.8*" /><ColumnDefinition Width="1.2*" /></Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="TYPE" Foreground="{StaticResource TextMutedBrush}" FontSize="9" />
                                                <TextBlock Text="{Binding Type}" FontSize="12" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock Text="SOUND PACK" Foreground="{StaticResource TextMutedBrush}" FontSize="9" />
                                                <TextBlock Text="{Binding PackAvailability}" FontSize="12" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <StackPanel Grid.Column="1">
                    <Border Style="{StaticResource CardStyle}" Height="205" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="Connect vAMSYS" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="Import virtual airline memberships and assigned fleets through secure browser authorization."
                                       Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,8,0,0" />
                            <TextBlock Text="STATUS" Foreground="{StaticResource TextMutedBrush}" FontSize="9" Margin="0,12,0,0" />
                            <TextBlock Text="Developer registration required" Foreground="{StaticResource WarningBrush}" FontSize="12" Margin="0,3,0,10" />
                            <Button Content="Set up vAMSYS" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ConnectVamsysCommand}" MinHeight="38" Padding="12,5" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Height="181" Padding="18,14" Margin="0,14,0,0">
                        <StackPanel>
                            <TextBlock Text="Custom Airline" Style="{StaticResource SectionTitleStyle}" />
                            <TextBlock Text="Create a local airline profile and pair it with your own cabin sound pack."
                                       Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,8,0,0" />
                            <TextBlock Text="Content-pack profile builder" Foreground="#C77CF4" FontSize="12" Margin="0,14,0,12" />
                            <Button Content="Create Profile" Style="{StaticResource SecondaryButtonStyle}" Foreground="#D68BFF" BorderBrush="#9E59C4" Command="{Binding CreateProfileCommand}" MinHeight="38" Padding="12,5" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="7">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                    <TextBlock Text="{Binding AvailableProfileCount}" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text=" airline profiles available  ·  " Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding InstalledPackCount}" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text=" sound pack installed" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
                <TextBlock Text="{Binding SelectionStatus}" Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
