<UserControl x:Class="FreeFlight.CabinControl.App.Views.LiveCabinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:FreeFlight.CabinControl.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="820"
             d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="108" />
        </Grid.RowDefinitions>

        <views:PassengerFlowView Grid.Row="0" DataContext="{Binding Passengers}" />

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="190" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AIRCRAFT DOORS / EXITS" Foreground="#61758E" FontSize="9" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding DoorSummary}" Foreground="#C8D6E7" FontSize="12" Margin="0,6,10,0" TextWrapping="Wrap" />
                </StackPanel>

                <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Doors}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="122" Height="76" Margin="0,0,8,0" Padding="10,8" Background="#0A1728" BorderBrush="#29415F" BorderThickness="1" CornerRadius="5">
                                    <Grid>
                                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                        <Grid>
                                            <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="Bold" Foreground="#EAF3FF" />
                                            <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                        </Grid>
                                        <TextBlock Grid.Row="1" Text="{Binding KindLabel}" Foreground="#71869F" FontSize="9" Margin="0,3,0,0" />
                                        <TextBlock Grid.Row="2" Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Bottom" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>