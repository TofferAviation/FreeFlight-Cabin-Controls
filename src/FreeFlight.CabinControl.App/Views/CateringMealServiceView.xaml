<UserControl x:Class="FreeFlight.CabinControl.App.Views.CateringMealServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="820"
             d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="112" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Reload catering" Command="{Binding StartLoadingCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" />
                <Button Content="Refill inventory" Command="{Binding RefillCommand}" Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="CATERING LOAD" Style="{StaticResource MutedTextStyle}" />
                    <TextBlock Text="{Binding CateringProgress}" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,4,0,0" />
                    <TextBlock Text="{Binding InventoryStatus}" Foreground="#8DA0B8" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="ONBOARD REVENUE" Style="{StaticResource MutedTextStyle}" />
                    <TextBlock Text="{Binding FlightRevenue}" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,4,0,0" />
                    <TextBlock Text="{Binding PurchaseSummary}" Foreground="#8DA0B8" FontSize="12" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="8,0,0,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="LIVE SERVICE MODEL" Style="{StaticResource MutedTextStyle}" />
                    <TextBlock Text="Inventory changes progressively" FontSize="16" FontWeight="SemiBold" Margin="0,7,0,0" />
                    <TextBlock Text="Loading, refills and passenger purchases update stock in real time." Foreground="#8DA0B8" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*" /><ColumnDefinition Width="0.85*" /></Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0" Padding="18">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="40" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <TextBlock Text="Live catering inventory" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="LOADED / TARGET" HorizontalAlignment="Right" Foreground="#61758E" FontSize="10" VerticalAlignment="Center" />
                    </Grid>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding InventoryRows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="0,0,0,1" Padding="0,9">
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="95" /><ColumnDefinition Width="105" /><ColumnDefinition Width="105" /></Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding Category}" Foreground="#71869F" FontSize="10" Margin="0,2,0,0" />
                                                <ProgressBar Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="4" Margin="0,6,22,0" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding Price}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#C7D5E5" />
                                            <TextBlock Grid.Column="2" Text="{Binding State}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#69B8FF" FontSize="11" />
                                            <TextBlock Grid.Column="3" Text="{Binding QuantityLabel}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0,0,0" Padding="18">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="40" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <TextBlock Text="Passenger onboard spend" Style="{StaticResource SectionTitleStyle}" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding PassengerSpendRows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="54" /><ColumnDefinition Width="*" /><ColumnDefinition Width="90" /></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding SeatNumber}" FontWeight="Bold" Foreground="#69B8FF" VerticalAlignment="Center" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding PassengerName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding PurchaseCountLabel}" Foreground="#71869F" FontSize="10" Margin="0,2,0,0" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding TotalSpendLabel}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>