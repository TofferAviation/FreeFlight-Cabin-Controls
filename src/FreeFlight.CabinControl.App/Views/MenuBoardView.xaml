<UserControl x:Class="FreeFlight.CabinControl.App.Views.MenuBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="820"
             d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="&#xE76B;" FontFamily="Segoe Fluent Icons" Command="{Binding PreviousPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="44" Height="36" ToolTip="Previous menu page" />
                <TextBlock Text="{Binding PageNumberLabel}" Margin="14,0" VerticalAlignment="Center" Foreground="#8FA5BE" FontWeight="SemiBold" />
                <Button Content="&#xE76C;" FontFamily="Segoe Fluent Icons" Command="{Binding NextPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="44" Height="36" ToolTip="Next menu page" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="430" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1"
                    Background="#F6F1E7"
                    BorderBrush="#D0C6B4"
                    BorderThickness="1"
                    CornerRadius="5"
                    Padding="34,28"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    MaxHeight="690">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="5" Opacity="0.32" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="1" />
                        <RowDefinition Height="22" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="24" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="BRITISH AIRWAYS" Foreground="#17365D" FontSize="12" FontWeight="Bold" LetterSpacing="1.5" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding CurrentPage.Eyebrow}" Foreground="#7A6F61" FontSize="9" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,13,0,0" />
                        <TextBlock Text="{Binding CurrentPage.Title}" Foreground="#172A43" FontFamily="Georgia" FontSize="31" FontWeight="Normal" HorizontalAlignment="Center" Margin="0,4,0,0" />
                    </StackPanel>

                    <Rectangle Grid.Row="2" Fill="#C6BBA8" />

                    <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding CurrentPage.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,19">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="76" /></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" Foreground="#1E2936" FontFamily="Georgia" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding Category}" Foreground="#887B69" FontSize="9" Margin="0,4,0,0" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding PriceLabel}" Foreground="#17365D" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Top" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <StackPanel Grid.Row="6" HorizontalAlignment="Center">
                        <TextBlock Text="Speedbird. Always." Foreground="#17365D" FontFamily="Georgia" FontSize="12" FontStyle="Italic" HorizontalAlignment="Center" />
                        <TextBlock Text="Menu availability follows the live catering inventory." Foreground="#887B69" FontSize="8" HorizontalAlignment="Center" Margin="0,5,0,0" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>